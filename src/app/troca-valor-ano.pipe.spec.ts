import { TrocaValorAnoPipe } from './troca-valor-ano.pipe';

describe('TrocaValorAnoPipe', () => {
  it('create an instance', () => {
    const pipe = new TrocaValorAnoPipe();
    expect(pipe).toBeTruthy();
  });
});
